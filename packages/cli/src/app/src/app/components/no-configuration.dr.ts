import { dictionaryRequirements } from "@/lib/utils/dictionary"

export const NoConfigurationDr = dictionaryRequirements({
  createYourConfiguration: true,
  fromTemplate: true,
  fromTemplateDescription: true,
  fromScratch: true,
  fromScratchDescription: true,
  suggested: true,
  advanced: true,
})
