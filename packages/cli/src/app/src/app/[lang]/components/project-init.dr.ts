import { dictionaryRequirements } from "@/lib/utils/dictionary"

export const ProjectInitDr = dictionaryRequirements({
  app: {
    name: true,
  },
  welcomeTo: true,
  enterYourProjectName: true,
})
