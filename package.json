{
  "name": "next-boilerplate",
  "private": true,
  "scripts": {
    "init": "cd scripts && npm i && npm run init && cd ../packages/app && cp .env.example .env && npx prisma migrate dev && npm run seed",
    "dev": "npm run dev --prefix packages/app"
  },
  "devDependencies": {},
  "workspaces": [
    "packages/*"
  ]
}
